@using Kendo.Mvc.UI
@{
    ViewBag.Title = "Home Page";
}
@(Html.Kendo().ButtonGroup()
                            .Name("player")
                            .Items(t =>
                            {
                                t.Add().Text("play");
                                t.Add().Text("pause");
                                t.Add().Text("stop");
                            }))